import React, { cloneElement } from 'react';
import ChatsListScreen from './components/ChatsListScreen';

const App: React.FC = () => (
  <div>
    <ChatsListScreen />
  </div>
);

export default App;
